# Negative results & fraud gallery — fdrs-star-quotients

Seeded **before the battery code** (ADR-007). Refuted claims recorded at full weight;
tempting-but-wrong spins named so they cannot be quietly used.

## Fraud gallery — spins this family must NOT be used to support

1. **"gauge = |det| = |group| (P1 generalizes)."** FALSE — the seeded fraud. For a quotient
   singularity `|det(resolution form)| = |H₁(S³/G)| = |G^{ab}|`, the **abelianization**. P1's
   `|det| = n = |group|` held only because cyclic groups are abelian. Witness: `2I` is perfect,
   `det(E₈) = 1` while `|2I| = 120`. Any statement or implication that `|det| = |G|` generalizes
   is `F-S-naive`.
2. **"Verified coker(form) ≅ H₁(link) by computing H₁ = coker(intersection matrix)."** CIRCULAR —
   that identity is **Mumford's theorem (1961)**; computing `H₁` from the matrix and comparing back
   proves nothing. The independent side, `G^{ab}`, MUST come from the **group presentation** (GAP),
   never off the graph (`F-S-circular`). H-S2 verifies Mumford's theorem at the group level by
   computing its two sides — `coker(plumbing)` (SNF) and `G^{ab}` (GAP) — independently.
3. **"The Seifert table says so."** A single classical table is not a source — transcription
   errors are a known hazard. Every table entry needs an **independent plumbing cross-check**
   (or it is not in the verified battery). Trusting a table alone is `F-S-tables`.
4. **"|det| = ∏nᵢ · |b − Σ qᵢ/nᵢ| — a discovery."** No — it is the classical star-shaped
   plumbing determinant (Neumann). The artifact is the **FDRS reading** (network gauge = arm
   gauges × orbifold Euler number) + the verified sweep, never new mathematics (`F-novel`).
5. **"The hub is a Phase-5.3 rechart / a digit of an arm."** Unforced. The central node is not
   a digit of any arm law; its FDRS placement is a candidate (coupling interface Def 193 vs a
   degenerate timeline), tested or recorded **open** — no forced fit (the P5 F5-transport lesson).
6. **"The whole |G| ≤ 500 classification is verified."** Only if each entry is cross-checked two
   ways. The verified core is the SL(2)/ADE quotients (plumbing + GAP both available) + the
   broad arbitrary-star H-S3 sweep; the general GL(2) non-Gorenstein tables are **deferred**
   (F-S-tables), not silently claimed.

## Falsifier table (PS battery landed 2026-07-07)

| ID | Fires when | Kills | Status |
|---|---|---|---|
| **F-S-naive** | stating/implying `\|det\| = \|G\|` generalizes | the gauge law | **CONFIRMED FIRING** — `\|det\|=\|G\|` fails on all 5 nonabelian core entries; the truth is `\|G^ab\|` (E₈: `1≠120`) |
| **F-S-circular** | `G^{ab}` taken from `coker(intersection matrix)` not the presentation | H-S2's independence | **HONORED** — H-S2 compares `coker(plumbing)` (graph: SNF) to `G^ab` (group: GAP `AbelianInvariants`) as **two independently computed objects**; the theorem `coker ≅ G^ab` is verified, never assumed |
| **F-S-tables** | a Seifert entry trusted without an independent plumbing cross-check | the battery's integrity | **HONORED** — general GL(2) tables **deferred**; verified core is SL(2)/ADE (plumbing + GAP both) + the group-free H-S3 sweep |
| **F-exact** | any float in det / gauge / group arithmetic | the `exact` tag | **SILENT** — ℤ/ℚ only; Rust Bareiss fraction-free; `b` via integer ceil |
| **F-novel** | claiming new singularity/group mathematics | the honest-scope contract | **HONORED** — Brieskorn/Riemenschneider/Neumann/ADE classical; Thm-89 reading cited |

## Recorded negatives (bounded-completeness statements)

- **General GL(2,ℂ) non-Gorenstein battery — DEFERRED (F-S-tables), with a known unlock (PS.5).**
  The verified group-connected core is the SL(2)/ADE quotients (`A_n`, `D_n`, `E₆/E₇/E₈` =
  `2T/2O/2I`), where the plumbing AND the group are independently available. The general `D_{n,q}`
  and the `T/O/I` central extensions require the Riemenschneider tables; without an independent
  construction we do not admit them to the verified battery. Not a failure — a drawn boundary. The
  **unlock is in the toolkit**: derive the GL(2) resolution data (invariant theory / Riemenschneider
  dot-diagram) rather than copy it, so the tables become the thing *checked* not the input. That is
  **PS.5**, and it would exercise H-S2's isomorphism form on richer abelianizations (`C₂×C₄`,
  `C₃×C₃`) beyond the core's `{cyclic, C₂×C₂}`.
- **H-S4 hub placement — OPEN.** The central `−b` node is not a digit of any arm law; whether it
  is a Phase-14 coupling interface (Def 193) or a degenerate fourth timeline is recorded open, not
  forced (the P5 `F5-transport` lesson).
